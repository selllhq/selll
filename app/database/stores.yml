columns:
  name: string
  description: text
  status:
    type: string
    default: sandbox # sandbox or live
  identifier:
    type: string
    unique: true

relationships:
  - User

seeds:
  count: 5
  truncate: true
  data:
    name: '@faker.word'
    identifier: '@faker.words:3'
    verified_at: '@tick.format:YYYY-MM-DD HH:mm:ss'
