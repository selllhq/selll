columns:
  name: string
  description: text
  status:
    type: string
    default: sandbox # sandbox or live
  slug:
    type: string
    unique: true
  currency:
    type: string
    default: USD
  config:
    type: text
    nullable: true
  kyc:
    type: string
    nullable: true

relationships:
  - User
