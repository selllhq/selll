columns:
  rate: string
  remaining_transactions:
    type: string
    nullable: true
  resets_at:
    type: timestamp
    nullable: true

relationships:
  - Store

seeds:
  count: 5
  truncate: true
  data:
    name: '@faker.name'
    identifier: '@faker.uuid'
    verified_at: '@tick.format:YYYY-MM-DD HH:mm:ss'
