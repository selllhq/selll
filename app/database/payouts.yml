columns:
  amount: string
  currency: string
  selll_fee: string
  selll_fee_amount: string
  payment_fee: string
  payment_provider: string
  payment_fee_amount: string
  reference:
    type: string
    nullable: true

relationships:
  - Store
  - Wallet
  - Cart
