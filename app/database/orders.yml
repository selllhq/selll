columns:
  status:
    type: string
    default: pending

relationships:
  - Store
  - Customer
  - Product

seeds:
  count: 5
  truncate: true
  data:
    name: '@faker.name'
    identifier: '@faker.uuid'
    verified_at: '@tick.format:YYYY-MM-DD HH:mm:ss'
