columns:
  total: string
  currency: string
  billing_session_id:
    type: string
    nullable: true
  store_url:
    type: string
    nullable: true
  items:
    type: json
    nullable: true
  status:
    type: enum
    default: pending
    values:
      - pending
      - paid
      - completed
      - failed
      - cancelled
      - abandoned

relationships:
  - Customer
  - Store
