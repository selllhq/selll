columns:
  items: text
  total: string
  billing_session_id:
    type: string
    nullable: true
  store_url:
    type: string
    nullable: true
  status:
    type: string
    default: pending

relationships:
  - Customer
  - Store
