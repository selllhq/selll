columns:
  name: string
  email:
    type: string
    unique: true
  phone:
    type: string
    nullable: true
  options: text

seeds:
  count: 5
  truncate: true
  data:
    name: '@faker.name'
    identifier: '@faker.uuid'
    verified_at: '@tick.format:YYYY-MM-DD HH:mm:ss'
