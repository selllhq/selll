columns:
  name: string
  email:
    type: string
  phone:
    type: string
    nullable: true
  address:
    type: string
    nullable: true
  city:
    type: string
    nullable: true
  country:
    type: string
    nullable: true
  notes:
    type: text
    nullable: true

relationships:
  - Store

seeds:
  count: 5
  truncate: true
  data:
    name: '@faker.name'
    slug: '@faker.uuid'
    verified_at: '@tick.format:YYYY-MM-DD HH:mm:ss'
